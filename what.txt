<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <title>100+ Free Games</title>
  <style>
    body{font-family:system-ui,Arial;background:#111;color:#eee;margin:0;padding:2rem}
    #search{width:100%;max-width:400px;padding:.6rem 1rem;font-size:1.1rem;border:none;border-radius:4px;margin-bottom:1.5rem}
    #grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(150px,1fr));gap:1rem}
    .card{background:#222;border-radius:6px;overflow:hidden;text-align:center;text-decoration:none;color:#fff;display:block;transition:transform .15s}
    .card:hover{transform:scale(1.05)}
    .card img{width:100%;height:100px;object-fit:cover;background:#333}
    .card span{display:block;padding:.5rem;font-size:.9rem}
  </style>
</head>
<body>
  <h1>Play 100+ Games Instantly</h1>
  <input id="search" type="text" placeholder="Search games…" autocomplete="off"/>
  <div id="grid"></div>
  <script>
    fetch('games.json')
      .then(r=>r.json())
      .then(draw);
    function draw(list){
      const grid=document.getElementById('grid'),search=document.getElementById('search');
      function render(filter=''){
        grid.innerHTML='';
        list.filter(g=>g.title.toLowerCase().includes(filter.toLowerCase()))
            .forEach(g=>{
              const card=document.createElement('a');
              card.className='card'; card.href=g.file; card.target='_blank';
              card.innerHTML=`<img src="${g.thumb}" onerror="this.src='assets/placeholder.png'"/><span>${g.title}</span>`;
              grid.appendChild(card);
            });
      }
      render();
      search.addEventListener('input',e=>render(e.target.value));
    }
  </script>
</body>
</html>
